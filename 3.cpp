#include <iostream>
#define N 10
using namespace std;

int func(int i, int x) {
    while(i<=x && cout<<i<<endl && i++ ){}
}
int main(int i, char **c){
    if( ( i && i > 1 && func(1,i)) || (i==1 && main(N, c)))
    
    {}
        
}
